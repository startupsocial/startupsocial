<%= form_for(@signup) do |f| %>
  <div class="field">
    <%= f.text_field :name, :placeholder => "Name" %>
  </div>  
  <div class="field">
    <%= f.text_field :email, :placeholder => "Email" %>
  </div>
  <div class="actions">
    <%= f.submit :class => 'btn' %>
  </div>
<% end %>
